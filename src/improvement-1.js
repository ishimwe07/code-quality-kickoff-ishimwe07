// The following line contain the regular expression which check for letters matching "R" in given string.
const countUpperCaseRs = letters => letters.match(/R/g).length;

export default countUpperCaseRs;